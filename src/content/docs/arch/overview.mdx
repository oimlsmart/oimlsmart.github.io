---
title: 'Architecture Overview'
shortTitle: 'Overview'
---

import TreeDiagram from '../../../components/diagrams/TreeDiagram.astro'

The OIML SMART platform is built around a six-layer entity model. Each layer has well-defined entity types, a clear purpose, and a specific stage in the lifecycle of a SMART Recommendation.

## The six layers

| Layer | Entity Types | Purpose | R 60 Example |
| --- | --- | --- | --- |
| 1. Requirements | `RequirementClass`, `Requirement` | Normative provisions with obligation (shall/should/may) | "Errors shall not exceed MPE" |
| 2. Conformance Tests | `ConformanceTestClass`, `ConformanceTest` | Test procedures with variables, steps, criteria | Measurement error & repeatability test |
| 3. Forms | `Form`, `Field` | Structured data capture with measurement kinds | Load cell error recording form |
| 4. Form Instances | `FormInstance` | Runtime measurements stored in IndexedDB | A specific TL's filled-in form |
| 5. Evaluation Results | `EvaluationResult`, `FormDetermination` | Pass/fail with evidence chain | "PASS: E = 0.3d ≤ MPE = 0.5d" |
| 6. Certification | `Application` → `Certificate` | End-to-end workflow orchestration | Certificate `DE1-2024-R60-001` |

## Entity Relationships

The layers are connected through explicit references. Tests target requirements many-to-many, forms link to tests, and runtime instances reference both their schema and their workflow context.

<TreeDiagram
  caption="FIG. ENTITY RELATIONSHIP GRAPH"
  accent="brand"
  nodes={[
    { label: 'Standard', kind: 'root', type: 'BUILD', children: [
      { label: 'RequirementClass', annotation: 'via Standard', type: '1:n' },
      { label: 'Requirement', annotation: 'via RequirementClass', type: '1:n' },
      { label: 'ConformanceTestClass', type: '1:n' },
      { label: 'ConformanceTest', annotation: 'via ConformanceTestClass', type: '1:n' },
      { label: 'Form', type: '1:n', children: [
        { label: 'Field', type: '1:n' },
      ]},
      { label: 'Requirement ↔ ConformanceTest', annotation: 'targets', type: 'm:n', accent: 'teal' },
      { label: 'ConformanceTest ↔ Form', annotation: 'result_forms', type: 'm:n', accent: 'teal' },
    ]},
    { label: 'Runtime', kind: 'root', type: 'IDB', accent: 'amber', children: [
      { label: 'FormInstance', annotation: '→ Form, → ConformanceTest, → TestReport' },
      { label: 'EvaluationResult', annotation: '→ FormInstance → Requirement (traceability)' },
      { label: 'Application', annotation: '→ InstrumentModelFamily → ClassificationGroup → InstrumentModel → InstrumentSample' },
      { label: 'TestRequest', annotation: '→ Application, → InstrumentSample' },
      { label: 'TestReport', annotation: '→ TestRequest, → FormInstance[]' },
      { label: 'EvaluationReport', annotation: '→ TestReport, → FormDetermination[]' },
      { label: 'Certificate', annotation: '→ InstrumentModelFamily' },
    ]},
  ]}
/>

## Data Flow

Layers 1–3 are **build-time** YAML artifacts — they define the structure and rules. Layers 4–6 are **runtime** entities that live in IndexedDB in the browser. The evaluation pipeline processes runtime data against build-time declarations.

<ol class="pipeline-steps list-none grid gap-2 my-8" style="grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); counter-reset: pipeline;">
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>LOAD</strong> — Retrieve form schema and ConformanceTest definition
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>BIND</strong> — Resolve computation context from entity graph
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>POPULATE</strong> — Fill direct/measured fields with user data
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>DERIVE</strong> — Evaluate derived/computed fields (topological order)
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>EVALUATE</strong> — Apply OCL evaluation rules → pass/fail per field
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>AGGREGATE</strong> — Evaluate form-level pass_if → overall result
  </li>
  <li class="relative bg-paper-soft border border-rule rounded p-3 pl-12 text-sm leading-snug" style="counter-increment: pipeline;">
    <strong>TRACE</strong> — Link results back to Requirements (evidence chain)
  </li>
</ol>

<style is:global>{`
  .pipeline-steps li::before {
    content: counter(pipeline, decimal-leading-zero);
    position: absolute;
    left: 0.75rem;
    top: 50%;
    transform: translateY(-50%);
    font-family: var(--font-mono);
    font-size: 0.75rem;
    font-weight: 600;
    color: var(--color-brand-600);
    background: var(--color-brand-50);
    padding: 0.25rem 0.5rem;
    border: 1px solid var(--color-brand-400);
    border-radius: 2px;
    letter-spacing: 0.08em;
  }
`}</style>

## File Organization

<TreeDiagram
  caption="FIG. FILE ORGANIZATION"
  accent="teal"
  nodes={[
    { label: 'data/', kind: 'root', children: [
      { label: 'oiml-r60/', kind: 'branch', annotation: 'one directory per standard', children: [
        { label: 'standard.yaml', annotation: 'value model (tables, calculations)' },
        { label: 'dimensions.yaml', annotation: 'classification dimensions, parameters' },
        { label: 'requirements/', kind: 'branch', children: [
          { label: 'rc.yaml', annotation: 'requirements class schema' },
          { label: 'met-requirements.yaml', annotation: 'individual requirements' },
        ]},
        { label: 'conformance/', kind: 'branch', children: [
          { label: 'cc.yaml', annotation: 'conformance class schema' },
          { label: 'measurement-error-mdlo.yaml', annotation: 'individual tests' },
        ]},
        { label: 'forms/', kind: 'branch', children: [
          { label: 'measurement-error-mdlo.yaml', annotation: 'form schemas' },
        ]},
      ]},
      { label: 'oiml-r144/', kind: 'branch', annotation: 'another standard', children: [
        { label: '…', annotation: 'mirrors r60/ structure' },
      ]},
      { label: 'schemas/', kind: 'branch', annotation: 'JSON Schemas for validation' },
      { label: 'browser/src/data/generated/', kind: 'branch', annotation: 'TypeScript generated from YAML' },
    ]},
  ]}
/>

The build pipeline reads all YAML, validates schemas and cross-references, and generates TypeScript modules that the application imports at runtime.

## What's modelled in Primmel

The information model that defines these layers is authored in [Primmel](https://www.primmel.org/), Ribose's successor to MMEL. Primmel provides:

- **Data model declarations** for entities, attributes, and relationships.
- **Process model declarations** for state machines and transitions.
- **Compliance rule declarations** for typed expressions and OCL constraints.

The Primmel model is the canonical source; the YAML files above are its concrete serialization for a specific Recommendation.

Continue with the [design principles](/docs/arch/design-principles) that shape every decision in the model.
