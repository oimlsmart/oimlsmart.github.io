---
// ContentPage — shared layout for about/recommendations/library pages.
// These sections all follow the same pattern: read a content collection,
// filter by prefix, render with PageHero + DraftCallout.
import Base from './Base.astro'
import PageHero from '../components/PageHero.astro'
import DraftCallout from '../components/DraftCallout.astro'
import { render } from 'astro:content'

const { entry } = Astro.props
const { Content } = await render(entry)
const { title, description, eyebrow } = entry.data
---

<Base title={title} description={description}>
  <article class="prose">
    <PageHero eyebrow={eyebrow} title={title} lede={description} />
    <DraftCallout />
    <Content />
  </article>
</Base>