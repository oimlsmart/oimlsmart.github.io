---
// ContentPage — shared layout for about/recommendations/library/blog pages.
// Prose typography is provided by Base.astro's <style is:global> block;
// this layout just wraps content in a styled <article class="prose">.
import Base from './Base.astro'
import PageHero from '../components/PageHero.astro'
import DraftCallout from '../components/DraftCallout.astro'
import { render } from 'astro:content'

const { entry } = Astro.props
const { Content } = await render(entry)
const { title, description, eyebrow } = entry.data
---

<Base title={title} description={description}>
  <article class="prose">
    <PageHero eyebrow={eyebrow} title={title} lede={description} />
    <DraftCallout />
    <Content />
  </article>
</Base>